issue: 551
bump: patch

The floating tab bar's bottom clearance now tracks its actual rendered height instead of a fixed 120px guess, so a taller bar (larger text, changed labels) no longer overlaps the last scroll item or leaves an oversized gap.
