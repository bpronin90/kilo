issue: 577
bump: patch

Fixed the idle rest-timer "start" row on the Log screen being hidden behind
the floating bottom tab bar (and the bottom safe-area inset on notched
devices). It now reserves the same tab-bar/safe-area clearance already
applied to the running/done timer surface and the PR-moment banner.
