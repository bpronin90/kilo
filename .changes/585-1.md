issue: 585
bump: patch

Teach literal, typable workout syntax in the Log empty state instead of showing only a parsed preview. The example text is shared with regression tests to prevent drift between displayed guidance and parser expectations.
