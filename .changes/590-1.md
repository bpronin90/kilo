issue: 590
bump: patch

Fixed the workout day reminder so it reschedules automatically after the active routine's days, contents, or selection change — from any screen, and after an app restart — instead of staying pinned to whatever routine was active when the Settings screen was last opened.
