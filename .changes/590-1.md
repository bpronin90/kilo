issue: 590
bump: patch

Fixed the workout day reminder so it reschedules automatically after the active routine's days, contents, or selection change, instead of staying pinned to the routine that was active when the Reminders card first loaded.
