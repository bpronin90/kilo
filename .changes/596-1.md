issue: 596
bump: patch

Fixed weight-entry add and edit so a rejected save or update (a false return
or a thrown rejection) shows a retryable error instead of silently failing;
a failed edit now stays open with the entered values intact instead of
closing.
