issue: 607
bump: patch

Fixed corrupt on-device list storage (malformed or non-array JSON) being treated as an empty dataset, which let the next save silently overwrite the salvageable data; corrupt reads now surface the recoverable load-error banner instead.
