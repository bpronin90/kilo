issue: 615
bump: minor

Render workout star marks (e.g. `*PR`) and stored `--` comments in the Log read view as accessible, muted annotations beneath their set row, without affecting counted sets or canonical exercise names.
