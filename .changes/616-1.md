issue: 616
bump: minor

Surface workout-note parser errors in the Log read view: unrecognized set rows now show a warning glyph, the actionable parser message, and a screen-reader label naming the raw line and its recovery (no longer red text alone), and a whole-note rejection such as an oversize note renders a visible parse-failure banner instead of a blank read view.
