issue: 710
bump: patch

Fixed the Log routine-card headers so a long title or a full set of action pills no longer collapses the title to zero width; the title now truncates after two lines and the action row wraps instead of overflowing.
