issue: 737
bump: minor

Home, Log, and Weight now show a loading placeholder instead of a blank screen
while data is still being read, and Home, Log, Weight, and Analytics show a
retry banner when a read fails instead of presenting an empty or zeroed screen
as if it were real. Home also shows when changes are saved on this device and
waiting for Cloud Sync, with a link to Cloud Sync and a retry after a failed
sync.
