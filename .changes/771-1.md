issue: 771
bump: patch

Home's `1K Progress` total is back to its full-scale, polished typography instead of the smaller compact size it picked up during recent handoff work. Log's `Recovery` and `More Routines` headings continue to share the same section-title treatment.
