issue: 804
bump: patch

Simplified the Log tab's active Recovery panel: the week row no longer repeats
the card's headline state, the expected next action is the only highlighted
button, and `Manage recovery block` uses the standard chevron.
