issue: 809
bump: patch

Home now opens much faster after a cold launch. The one-time encrypted-storage migration no longer rescans every stored key on every app start, and Home's weight and workout data now paints from the device immediately instead of waiting on the initial background cloud sync.
