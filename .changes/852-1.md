issue: 852
bump: patch

Fixed workout-note entry so a newly logged set honors your selected weight
unit instead of always being read as lb: typing a load while in kg mode now
stores and derives the correct value everywhere (Home, Analytics, Recovery).
Previously saved rows are never reinterpreted.
