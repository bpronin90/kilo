issue: 856
bump: minor

Log editor: syntax and session-alignment problems in a workout note are now
surfaced inline while typing, with a compact error/warning count and Next/Prev
controls that jump the cursor straight to the offending line, no more digging
through the text or leaving edit mode. Validation is debounced so retyping a
large note stays responsive, and nothing about the authored text is ever
rewritten.
