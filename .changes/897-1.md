issue: 897
bump: patch

Logging a same-day weigh-in now only requires the weight field and Save
weigh-in. The optional Note and Date controls moved to compact "Note · " and
"Date · " rows below Save — still one tap away, still fully editable when
correcting a history entry, just no longer on screen by default.
