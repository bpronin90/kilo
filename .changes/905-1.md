issue: 905
bump: patch

Log's editor-header controls and `New routine` now present a 44dp tap target, the
read-view `Done` announces itself as a button, and the editor header wraps instead
of pushing a control off the screen edge at small widths or large text sizes.
