issue: 925
bump: patch

Updated the mobile app's build-tooling dependencies to a patched Browserslist
release, closing two high-severity advisories in the transitive dependency
used to target supported browsers during builds. No app behavior changes.
