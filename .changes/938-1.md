issue: 938
bump: minor

The workout-note editor now shows a one-tap row of digits, space, comma,
hyphen, asterisk, and newline keys while the note is focused, so logging sets
no longer needs a keyboard-plane switch. The Recovery inline note editor gets
the same row.
