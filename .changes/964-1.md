issue: 964
bump: patch

Fixed routine ordering in CSV export. Titles now sort by Unicode code point.
Previously they were compared as UTF-16 code units, which placed emoji and other
astral characters before private-use and other high BMP characters instead of
after them.
