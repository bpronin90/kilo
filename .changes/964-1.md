issue: 964
bump: patch

Fixed CSV export ordering routines by UTF-16 code units instead of Unicode code
points, so titles containing emoji or other astral characters sorted before
private-use and other high BMP characters rather than after them.
