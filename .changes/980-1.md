issue: 980
bump: patch

Added an on-device development client for live UI iteration. Preview builds move
to runtime preview-7, so existing preview installs must be replaced with a fresh
build to keep receiving updates.
