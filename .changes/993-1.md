issue: 993
bump: patch

Run the shared Edge Function module tests (security-event, rate-limit, and
health-data-scope) in pull-request and `main` CI, and guard rate-limit event
classification with behavioral assertions: an exhausted bucket must emit the
subject-attributed throttle event and a limiter outage must emit only
`ratelimit.unavailable`.
