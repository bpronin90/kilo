issue: 997
bump: patch

Retrying a routine save after a sync hiccup no longer creates a duplicate
routine: if the on-device save succeeded but the cloud queue write failed, the
retry finishes that same routine instead of adding a second copy.
