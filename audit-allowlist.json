{
  "accepted": [
    {
      "id": "GHSA-w3rx-r6r6-pgpr",
      "package": "image-size",
      "reviewBy": "2026-10-07",
      "reason": "Re-checked 2026-09-07: still no patched release. Both advisories cover image-size <=2.0.2, 2.0.2 (published 2025-04-02) remains the latest version on npm, and GitHub's advisory pages list patched version as none. In mobile's current tree it resolves via expo@54.0.35 -> @expo/metro@54.2.0 -> metro@0.83.3 -> image-size@1.2.1, still build-machine-only tooling never bundled into the shipped app. npm audit's only fixAvailable is a semver-major expo bump to 57.0.20, which is a breaking upgrade far larger than the risk and out of scope for a routine dependency bump. Revisit when image-size publishes a fix or Expo bumps its metro pin off the affected range."
    },
    {
      "id": "GHSA-5p2g-fcmc-qvqq",
      "package": "image-size",
      "reviewBy": "2026-10-07",
      "reason": "Same unpatched image-size <=2.0.2 dependency as GHSA-w3rx-r6r6-pgpr, differing only in which parsers loop (JXL and HEIF rather than ICNS). Re-checked 2026-09-07 alongside it: no fix shipped, same expo@54.0.35 -> @expo/metro@54.2.0 -> metro@0.83.3 -> image-size@1.2.1 path, same breaking-major-only remediation. Build-time only, not shipped to devices. Tracked and revisited together."
    }
  ]
}
